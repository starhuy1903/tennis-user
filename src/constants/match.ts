export enum MatchParticipantStatus {
  PLAYED = 'played',
  NO_PARTY = 'no_party',
  NO_SHOW = 'no_show',
  WALK_OVER = 'walk_over',
}

export enum MatchState {
  SCHEDULED = 'scheduled',
  NO_PARTY = 'no_party',
  NO_SHOW = 'no_show',
  WALK_OVER = 'walk_over',
  DONE = 'done',
  SCORE_DONE = 'score_done',
}
